# Read first — AEIB v0.1 research branch

This branch adds a product-neutral defensive benchmark and does not modify ECO application source.

Before any merge decision:

1. inspect the exact changed-file set;
2. require the dedicated AEIB qualification workflow to pass on the PR checkout;
3. verify the workflow publishes no artifact;
4. keep the benchmark's safety boundary and non-claims intact;
5. do not describe the 22-case v0.1 foundation as equivalent to the much larger historical hostile campaign;
6. preserve the initially failed local determinism test as part of the engineering history rather than claiming first-pass success.

A merge would make only the benchmark source/specification public. Generated corpus material should remain generated on demand for this first tranche.
